<h1>Отложенные</h1>
<% @cart.orderables.each do |orderable| %>
  <% sport = orderable.sport %>
  <p><%= sport.title %> * <%= orderable.quantity %> = $<%= sport.price * orderable.quantity %></p>

       <%= render partial: "cart/cart" if @render_cart %>
<% end %>
<p>Ваша общая сумма за тренировки (в рублях)  <%= @cart.total %> </p>
<style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    h1 {
      color: #333;
      background-color: #fff;
      padding: 20px;
      margin: 0;
      text-align: center;
      border-bottom: 1px solid #ddd;
    }

    p {
      margin: 10px 0;
      padding: 5px;
    }

    strong {
      color: #007BFF;
    }

    a {
      color: #007BFF;
      text-decoration: none;
      display: inline-block;
      margin-top: 10px;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>